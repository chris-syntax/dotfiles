# login.nu — macOS login-shell extras  (chezmoi: darwin only)
#
# Sourced only for `nu --login` (Terminal, SSH, Alacritty with args=["--login"]),
# AFTER env.nu and config.nu. Environment and integrations are already handled
# there, so this is intentionally empty — a home for future login-only tweaks.
