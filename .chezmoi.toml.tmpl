{{- $ownership := promptChoiceOnce . "ownership" "Is this a work or personal machine" (list "personal" "work") -}}
[data]
ownership = {{ $ownership | quote }}

[cd]
command = "{{ if eq .chezmoi.os "darwin" }}/opt/homebrew/bin/nu{{ else }}/usr/bin/nu{{ end }}"
